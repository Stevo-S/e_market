<div class="container">
  <%= simple_form_for @category, html: {class: 'form-horizontal'} do |f| %>
  
  <%= f.input :name %>
  <%= f.select :parent_id, nested_set_options(Category) {|i| "#{'–' * i.level} #{i.name}" }, prompt: 'Choose Parent Category' %>
  
  <%= f.submit 'Create New Category', class: 'btn btn-default btn-lg' %>
  
  <% end %>
</div>