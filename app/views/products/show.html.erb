<div class="container">
  <div class="row">
    <div class="col-md-5">
      <%= image_tag(@product.picture_url(:list_version).to_s) if @product.picture? %>
    </div>
    <div class="col-md-5">
      <h2><%= @product.product_name %></h2>
      <p><%= @product.description %></p>
    </div>
  </div>
</div>
